# Firmware

This directory is reserved for firmware research and verified firmware metadata.

Before considering any image usable, record:

    filename
    file size
    SHA256
    source / provenance
    intended flash method
    expected partition target
    hardware compatibility
    test status

Do not blindly flash unknown full dumps.

Do not replace Factory / EEPROM / calibration regions with data from another router.
